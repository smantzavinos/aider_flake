[project]
name = "aider-flake"
version = "0.1.0"
description = "Development environment for Aider"
requires-python = ">=3.11"
dependencies = [
    "aider-chat==0.80.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
